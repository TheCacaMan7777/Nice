let principal = prompt("What is the starting ammount in the account?");
let rate = prompt("What is the annual interest rate in percent?");
let time = prompt("What is the time for the investement in years");

let amount = principal * (1 + )/3;

console.log("The average of the scores " + firstNumber + ", " + secondNumber)